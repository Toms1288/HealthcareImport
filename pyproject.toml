[project]
name = "projet-5"
version = "0.1.0"
description = "Environnement pour l'import de données dans MongoDB"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "pymongo>=4.17.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
